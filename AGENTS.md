# AGENTS.md — theMobiusStrip profile

Guidance for AI agents working in this repo.
`CLAUDE.md` imports this file, so this is the single source of truth.

## What this is

Public GitHub profile repository for `theMobiusStrip`. `README.md` is the
published profile.

## Scope

- Keep the profile concise, factual, and evidence-based.
- Keep important positioning and selected work easy to scan.
- Link to public, canonical destinations.
- Do not add build tooling or dependencies without a concrete need.

## Verify

- Run `git diff --check` after edits.
- Review the complete README for flow and Markdown rendering.
- Verify any added or changed links.

## Public repo hygiene

Never commit private data, credentials, machine details, local usernames, or
absolute/internal filesystem paths. Prefer repo-relative paths in tracked files.

## Working style

- Prefer small, verifiable changes.
- Preserve the silent profile counter unless explicitly asked to change it.
- Commit or push only when explicitly asked; branch first if on the default branch.
